
## 📌 Backend (Spring Boot + Render + Supabase)

```markdown
# Query App Backend

This is the Spring Boot backend for the Query App. It provides REST APIs for the React frontend and connects to a **Supabase Postgres database**. Deployed on **Render**.

## 🚀 Features
- Spring Boot REST API
- JWT-based authentication (if added)
- Connects to Supabase Postgres
- Deployed on Render

## 🛠️ Tech Stack
- Java (Spring Boot)
- PostgreSQL (Supabase)
- Maven
- Render (deployment)

## ⚙️ Environment Variables
Set these in **Render → Environment**:

